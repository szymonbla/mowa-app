# Y2 - Independently review Codex YOLO launches

## Goal

Szymon receives an independent verdict that new mowa sessions use the requested
launch mode while preserving the existing dispatch contract.

## Workspace and dependency

Wait for Y1's completed handoff. Review the exact before/after artifacts and
diff under `/home/szymon/mowa/specs/codex-yolo/evidence/Y1/`, against the current
three files named in Y1. This launcher-only ticket overrides the product
detached-worktree and integration-branch procedure in `roles/_common.md`.
Read the live files without editing them; use `/tmp` for checks.

## Done when

- Review Y1 against its three acceptance conditions and record PASS or FAIL
  with concrete actionable findings and the exact reviewed artifact identity.
- Independently run shell syntax and relevant mocked argv checks; verify
  quoting, defaults/overrides, Herdr-only guards, and no conflicting flags.
- Record which checks were mocked versus live, and any live-start evidence
  supplied by Szef; do not claim a new launch changes existing sessions.

## Checks

`bash -n` for both launchers; inspect and re-run Y1's argument-capture harness.
Run `npm run typecheck` and `npm test` per the reviewer role. No product edits,
fixes, merges, pushes, or independent agent launches.

## Deliverable

Append PASS or FAIL and evidence to
`/home/szymon/mowa/specs/codex-yolo/notes.md`, then stop. A FAIL returns to
Szef for a bounded implementer follow-up; this reviewer never fixes the work.
