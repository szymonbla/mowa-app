# Y1 - Use the requested Codex launch mode

## Goal

New mowa coordinator and Herdr worker sessions start with `codex --yolo` so
Szymon's unattended workflow uses his requested execution mode.

## Workspace

This is launcher infrastructure, not product implementation. The allowed files
are `/home/szymon/mowa/scripts/agents/mowa-szef`,
`/home/szymon/mowa/scripts/agents/mowa-dispatch-herdr`, and
`/home/szymon/mowa/scripts/agents/README.md`.

For this ticket only, edit these existing untracked files in place; do not use
the product worktree/landing procedure in `roles/_common.md`. No integration
branch change, commit, push, or merge. Save before/after copies and a unified
diff under `/home/szymon/mowa/specs/codex-yolo/evidence/Y1/` for independent
review. Do not copy secrets or unrelated files.

## What is already known

Both launchers use workspace-write/on-request. The installed Codex accepts
`--yolo`; login is valid. Role/model defaults and overrides are documented in
the launcher README. Runtime commands are symlinks to these files.

## Done when

- Both launchers pass `--yolo` without conflicting sandbox/approval flags and
  preserve role/model/effort, ticket/prompt arguments, and Herdr-only guards.
- The launcher README accurately describes the new-session behavior and its
  effect on sandboxing/approvals, without changing merge or worker-count rules.
- Evidence includes before/after diff, shell syntax validation, and mocked
  argument-capture checks for both launchers including model/effort overrides
  and invalid invocations; notes name every check and its result.

## Checks

Run `bash -n` on both launchers. Use temporary stub commands to capture argv
without launching nested Codex agents or mutating live Herdr workspaces. Verify
the original goal and ticket text survive quoting and defaults/overrides remain
correct. Do not source shell text from user content. Run `npm run typecheck`
and `npm test` per AGENTS.md; identify failures caused by existing user changes
without fixing them. No Electron/Vite boundary changes, so packaging is not
required. Save the temporary verification harness with the evidence if useful.

## Scope edge

Only the two active launchers and their README. Do not modify product code,
legacy launchers, roles, global settings, credentials, or existing agent
permissions. Only Szef dispatches or closes live workers.

## Deliverable

Append the standard handoff to `/home/szymon/mowa/specs/codex-yolo/notes.md`,
including changed paths, evidence location, checks, and any blocker, then stop.
