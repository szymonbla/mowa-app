# C2 - First live Codex reviewer on ticket 01

## Goal

A Codex reviewer session reviews ticket 01 (provider catalog) and appends
PASS or FAIL to `specs/superwhisper-byok/notes.md`, proving the Codex path
end to end.

## Workspace

This is not a separate worker ticket. The worker is dispatched on the real
ticket `specs/superwhisper-byok/tickets/01-provider-catalog.md` with role
`reviewer` and kind `codex`, agent name `rev-01-codex`. This file records
what Szef checks on the way.

## What is already known

- The Claude reviewer rev-01 hit the session limit at 23:59 on 2026-09-11
  before writing a verdict. Its detached worktree
  `.claude/worktrees/review-01-provider-catalog` exists at 470d105.
- Ticket 01 landed as d0beef8..470d105 on `byok` (implementer notes, entry
  "2026-09-11 - implementer - 01-provider-catalog - impl-01").

## Done when

- Codex agent shows in `herdr agent list` as `rev-01-codex`, kind codex.
- It reads the role (its first commands touch the ticket and notes, not
  random files).
- `specs/superwhisper-byok/notes.md` gets a reviewer entry with PASS or FAIL.

## Scope edge

Szef does not judge the review content. If Codex writes a FAIL, the fixes go
back to an implementer ticket as usual.

## Evidence bar

`herdr agent list` line, the notes entry, and the pane output around the first
three commands.

## Deliverable

Szef appends the outcome to `specs/codex-workers/notes.md`.
