# H1 - Install the coordinator loop

## Goal

Szymon can start one coordinator for `mowa` on the cloud host and have all state
land in specs, tickets, notes, and build board files.

## Workspace

Main repo: `/home/szymon/mowa`

## What is already known

The cloud host has `claude`, tmux, Node, and isolated copies under
`/home/szymon/mowa-agents`. It does not currently have Herdr or Codex installed.

## Done when

- Root workflow files exist and are synced to the cloud host.
- `mowa-szef` is available in `/home/szymon/bin`.
- Role hooks are present under `.claude/`.

## Scope edge

This ticket creates the loop. It does not install Herdr or Codex.

## Evidence bar

Show the synced files and the command that would start Szef.

## Found, do not fix

Herdr is absent on the cloud host.

## Deliverable

Append handoff to `specs/herd-loop/notes.md`, then stop.
